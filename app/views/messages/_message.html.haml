.mainChat__messageList--detail 
  %ul
    %li.mainChat__messageList--contributor
      = message.user.name
    %li.mainChat__messageList--date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .mainChat__messageList--content
    - if message.body.present?
      %p 
        = message.body
    = image_tag message.image.url if message.image.present?